
6[Done]. Checkboxes to show hide Workspaces do not represent real state
[How] It looks like BCGPro cannot deal with more than two workspacebars, that's why git tree is simply Workspacebar2 with flag to render tree not showing list of repos
5. Split repos into All Public Private
4. Check this: http://www.semdesigns.com/Products/SmartDifferencer/
3[Done]. Download and insert an avatar pic!(very important)
2[Done]. Insert user's repos into view (temporarily later on the structure where and how the repos are presented most likely will change)

1. Write exception type for HTTPS_GIT_Client::user_repositories(const Git_User & user) const
0. Make sure that checks are made and no uninitialized/nullptr/empty struct is accessed